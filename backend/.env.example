# Backend environment (copy to .env and fill)

# Supabase
# URL of your Supabase instance, e.g. https://xxxx.supabase.co
SUPABASE_URL=
# Service role key is recommended for server-side inserts/selects
SUPABASE_SERVICE_ROLE_KEY=
# Optional: anon key if needed for limited operations
SUPABASE_ANON_KEY=

# CORS / Frontend
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000
FRONTEND_URL=http://localhost:3000
BACKEND_PORT=8000

# Google Earth Engine (Service Account)
# Service account email (e.g. gee-sa@project.iam.gserviceaccount.com)
GEE_SERVICE_ACCOUNT=
# Private key JSON string (paste the entire JSON) or set a secret manager reference
GEE_PRIVATE_KEY=

# Optional (usually not needed if SERVICE_ACCOUNT + PRIVATE_KEY are provided)
GEE_PRIVATE_KEY_ID=
GEE_CLIENT_EMAIL=
GEE_CLIENT_ID=
GEE_TOKEN_URI=https://oauth2.googleapis.com/token
